# 2026-05-23

## 咖啡品牌落地页 - 图片修复
- 原因：Unsplash 图片 URL 加载失败（photo ID 无效或外链被拦截）
- 方案：全部 6 张图片（产品卡片 3 张 + 详情页 3 张）替换为 `picsum.photos/seed/coffee-{eth,col,yn}/...`
- 保底：每张图片增加 `onerror` SVG inline fallback（含国旗 emoji + 产地英文名）
- 已推送至 `XYMovo/SYSU-TEST` → `https://xymovo.github.io/SYSU-TEST/`

## 咖啡品牌落地页 - 6大模块全面升级
- **产品架构**：从3款扩展到7款，分三级（单一产地×4 + 拼配×2 + 限量×1）
- **详情页重做**：参考 Blue Bottle/三顿半布局 → Hero大图+规格卡片+产地故事+风味图谱+品鉴诗歌+冲泡指南+规格选购
- **购物车增强**：时段问候语 + 满199免运费进度条 + 「你可能喜欢」推荐引擎
- **问卷系统**：12题咖啡偏好问卷（频率/烘焙/风味/冲泡/预算/品牌/渠道等），数据存 localStorage，管理面板查看+导出CSV
- **登录注册**：localStorage 认证（email+password），可选登录，未登录也能浏览
- **结算流程**：3步结账（收货地址→支付方式→确认订单），登录后方可结算
- 全部图片用 picsum.photos + SVG fallback，单文件 HTML 约 1100 行
- 已推送至 `XYMovo/SYSU-TEST` → `https://xymovo.github.io/SYSU-TEST/`

## 问卷管理网站 + 主站风格升级
- **问卷管理网站** (`survey-admin.html`)：独立页面，含仪表盘（统计+柱状图）、原始数据表格（搜索/排序/分页/编辑/删除）、深度分析（风味偏好云、交叉分析）、数据维护（CSV/JSON导入导出）
- 问卷数据与主站共享 `rbsurveys` localStorage key
- **主站升级** — 参考瑞幸/LK风格：
  - Hero 增加咖啡豆散落 SVG 动画背景
  - 新增系列导航大图（单产地/拼配/限量 三张 Luckin式分类卡）
  - 新增「Why RAW?」模块化卖点区（4大理由）
  - 产品卡片增加 ☕ 角标图标
  - 跳转导航新增「为何选择」锚点
- 两个文件均已推送至 `XYMovo/SYSU-TEST`

## Node.js 后端 + Admin 管理后台
- **server.js**：纯 Node.js 后端（无 npm 依赖），JSON 文件存储，端口 3000
  - API：`/api/register`, `/api/login`, `/api/me`（认证）
  - `/api/orders`（创建订单），`/api/admin/orders`（管理订单）
  - `/api/admin/users`, `/api/admin/products`, `/api/admin/surveys`
  - 静态文件服务：`server.js` 也托管 `index.html`/`admin.html`
- **admin.html**：管理后台（登录 admin@rawbean.com / admin123）
  - 仪表盘：总收入/订单数/用户数/产品数 + 收入趋势图 + 热销 TOP5
  - 订单管理：列表/筛选/状态变更（待付款→已付款→处理中→已发货→已完成）
  - 用户管理：列表/编辑/删除
  - 产品管理：CRUD（新增/编辑/删除）
  - 问卷管理：列表/查看/删除/导出 CSV
- **index.html 升级**：智能 API 模式（localhost 用后端 API，GitHub Pages 用 localStorage 降级）
  - `submitOrder` 调用 `/api/orders` 创建订单
  - `handleAuth` 调用 `/api/register` 或 `/api/login`
  - `submitSurvey` 同时提交到 `/api/surveys`
- 三文件已推送至 `XYMovo/SYSU-TEST` → `https://xymovo.github.io/SYSU-TEST/`
- 本地运行：`node server.js`，访问 `http://localhost:3000/admin.html` 进入管理后台
